{
  "name": "vericity-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "VERICITY on-chain audit trail (ComplaintRegistry.sol) and Hardhat tooling",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network hardhat",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "log:sepolia": "node scripts/logEvent.js",
    "history:sepolia": "node scripts/getHistory.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0"
  }
}
